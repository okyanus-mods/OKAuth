{
  "schemaVersion": 1,
  "id": "okauth",
  "version": "${version}",
  "name": "OKAuth",
  "description": "Offline mode authentication",
  "environment": "server",
  "depends": {
    "okyanus": ">=0.1.0",
    "okyanus-runtime": ">=0.1.0"
  },
  "entrypoints": {
    "server": [
      "club.issizler.okauth.OKAuthMod"
    ]
  }
}
